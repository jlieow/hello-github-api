name: Get github commit details
on:
  push:
    tags:
      - "v*"

jobs:
  get_github_details:
    runs-on: ubuntu-latest
    env:
      message: ${{ github.event.head_commit.message }}
      head_commit: ${{ github.event.head_commit }}
      event: ${{ github.event }}
      actor: ${{ github.actor }}
      actactor_idor: ${{ github.actor_id }}
    steps:
      - name: Echo env
        run: |
          echo "\nmessage: $message"
          echo "\nhead_commit: $head_commit"
          echo "\nevent: $event"
          echo "\nactor: $actor"
          echo "\nactor_id: $actor_id"
        shell: bash
